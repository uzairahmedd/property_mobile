import React from 'react'
import { View } from 'react-native'
import { RootTabScreenProps } from '../types'

export default function Screen({ navigation }: RootTabScreenProps<'Home'>) {
  return <View style={{}}></View>
}
